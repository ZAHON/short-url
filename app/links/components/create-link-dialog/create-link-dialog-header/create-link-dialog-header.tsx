'use client';
import { DialogHeader, DialogTitle, DialogXButton } from '@/components';

export function CreateLinkDialogHeader() {
  return (
    <DialogHeader>
      <DialogTitle>Create a new link</DialogTitle>
      <DialogXButton />
    </DialogHeader>
  );
}
