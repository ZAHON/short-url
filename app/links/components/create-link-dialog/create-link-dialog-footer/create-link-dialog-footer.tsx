'use client';
import { DialogFooter } from '@/components';
import { CreateLinkDialogConfirmButton } from '../create-link-dialog-confirm-button';

export function CreateLinkDialogFooter() {
  return (
    <DialogFooter>
      <CreateLinkDialogConfirmButton />
    </DialogFooter>
  );
}
